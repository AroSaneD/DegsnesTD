using UnityEngine;
using System.Collections;
using TDLibrary;
public class TestScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
	    Debug.Log(new Class1().GetFive());
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
